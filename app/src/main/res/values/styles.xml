<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/white</item>
        <!-- Atur latar belakang menjadi putih -->

        <!-- Atur warna teks menjadi hitam -->
        <item name="android:textColor">@android:color/black</item>
<!--         Atur warna aksen aplikasi (misalnya, warna button) -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Atur warna status bar jika diperlukan -->

    </style>

    <!-- Style for the letsStartBtn button -->
    <style name="BlackButton">
        <item name="android:background">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

<!--    <style name="SpinnerTheme" parent="Widget.AppCompat.Light.DropDownItem.Spinner">-->
<!--        <item name="android:colorBackground">@color/white</item>-->
<!--        <item name="android:popupBackground">@color/white</item>-->
<!--    </style>-->

</resources>
